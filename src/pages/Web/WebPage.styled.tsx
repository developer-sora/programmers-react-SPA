import { css } from "@emotion/react";

const productListStyle = css`
  display: flex;
  flex-wrap: wrap;
`;

const productTextStyle = css`
  display: flex;
  justify-content: space-between;
`;

export { productListStyle, productTextStyle };
